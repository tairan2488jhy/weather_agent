# 实现我的第一个agent 天气agent

# 目前使用的依赖：

openai
gradio
fastapi
uvicorn
requests
dotenv
logging
time
pytest

# 一键安装环境  python -m pip install -r requirements.txt

# 拆分项目遇到的问题

1. 包冲突
   pip list | findstr llm